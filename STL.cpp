#include<bits/stdc++.h>
using namespace std;

int main(){
    /*pair<int,int> p={1,2};
    cout<<p.first<<" "<<p.second<<endl;

    pair<int,pair<int,int>> p1 = {1, {2,3}};

    cout<<p1.first<<" "<< p1.second.second <<" "<<p1.second.first<<endl;

    pair<int ,int> arr[] = { {1,2}, {2,5} ,{5,1}};
    cout<<arr[1].second;*/


   /*deque<int>dq;
   dq.push_back(1); //{1}
   dq.emplace_back(2);//{1,2}
   dq.push_front(4);//{4,1,2}
   dq.emplace_front(3);//{3,4,1,2}
   dq.pop_back();//{3,4,1}
   dq.pop_front();//{4,1}
   dq.back();
   dq.front();
   cout<<dq[5]<<endl;*/


   /*stack<int> st;
   st.push(1);// {1}
   st.push(2); //{2,1}
   st.push(3); //{3,2,1}
   st.push(4); //{4,3,2,1}
   st.push(5); // {5,4,3,2,1}
   st.push(6); // {6,5,4,3,2,1}

   cout<<st.top(); // it only return the top element of the stack
   st.pop();        // it delete the top element in the stack
   st.size();
   st.empty();

   stack<int>st1,st2;
   st1.swap(st2);*/


   /*queue<int> q;
   q.push(1); //{1}
   q.push(2); // {1,2}
   q.push(3); // {1,2,3}

   q.back() += 5; // means the last element in the queue (3)

   cout<<q.back();
   cout<<q.front();

   q.pop(); // it will delete the first element of the queue

   cout<<q.front();*/


   




    

    return 0;
}
